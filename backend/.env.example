# MongoDB connection string
# Local:  mongodb://127.0.0.1:27017/voting_system
# Atlas:  mongodb+srv://<user>:<password>@cluster.mongodb.net/voting_system
MONGO_URI=mongodb://127.0.0.1:27017/voting_system

# Server port
PORT=5000

# JWT secret - use any long random string
JWT_SECRET=change_this_to_a_long_random_secret_string

# Face match sensitivity (lower = stricter). 0.55 - 0.6 is a good default.
FACE_MATCH_THRESHOLD=0.55

# Email (for OTP) - use a Gmail App Password, not your real password
# https://myaccount.google.com/apppasswords
# Used for LOCAL DEVELOPMENT ONLY (npm run dev) - Render's free tier blocks
# outbound SMTP (ports 25/465/587), so Gmail SMTP will NOT work once deployed.
EMAIL_USER=youraddress@gmail.com
EMAIL_PASS=your_16_char_app_password

# Brevo (Sendinblue) transactional email API key - REQUIRED for production
# (Render/most free hosts). Sends over HTTPS (443) instead of raw SMTP, so
# it isn't blocked. Free tier: 300 emails/day. Get a key at:
# https://app.brevo.com/settings/keys/api
# Leave unset for local dev to keep using Gmail SMTP automatically.
BREVO_API_KEY=

# Admin bootstrap credentials (used once to create the first admin)
ADMIN_EMAIL=admin@votingsystem.com
ADMIN_PASSWORD=Admin@123
